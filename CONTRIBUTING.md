# 贡献指南

感谢你愿意改进 FocusBoard。我们尤其欢迎能让工具更安静、更清楚、更尊重用户数据的贡献。

## 开始前

1. Fork 仓库并创建描述清楚的分支，例如 `feat/task-export` 或 `fix/timer-reset`。
2. 安装 Node.js 22+，运行 `npm install` 和 `npm run dev`。
3. 保持界面在桌面和窄屏下都可用，避免引入不必要的账户、分析或远程依赖。

## 提交 Pull Request

- 一个 PR 聚焦一个可审阅的目的。
- 在提交前运行 `npm run build`。
- 说明用户会看到的变化，并附上界面截图（如涉及视觉变更）。
- 新功能请说明隐私影响；FocusBoard 默认坚持 local-first。

## 设计原则

- **少即是多**：优先删除干扰，而非增加控制项。
- **数据属于用户**：默认在本地存储，导出应始终可用。
- **可访问**：为图标按钮提供名称，保证键盘和小屏幕可操作。

参与即表示你同意遵守本项目的 [行为准则](CODE_OF_CONDUCT.md)。
