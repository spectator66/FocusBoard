# 隐私说明

FocusBoard 的核心功能运行在浏览器中。任务、每日意图、专注轮次和外观偏好以 `localStorage` 形式保存在当前浏览器和当前设备上。

- 应用不创建账户、不包含分析 SDK，也不会主动将这些数据上传到服务器。
- “导出数据”会在你的设备上生成一个 JSON 文件；该文件由你自行保管。
- 清除站点数据、使用无痕窗口或更换浏览器会移除本地数据。请在清理前导出备份。
- Google Fonts 仅用于界面字体加载；若需要完全离线或更严格的网络控制，可在贡献中将字体替换为本地资源。

这是一个开源项目，不提供数据恢复服务。
